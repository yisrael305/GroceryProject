#pragma once
#ifndef Validation
#define Validation
#include <stdio.h>
#include <ctype.h>


int validName(char** name);
int validID(char** ID);
int validPhone(char** phone);
int validNumber(char** number);
int validDate(char** date);
#endif

